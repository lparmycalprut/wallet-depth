# JANGAN commit API key!
config.json

# Python
__pycache__/
*.pyc
.cache/
.handoff_extract/
.venv/
venv/
.venv-test/

# Streamlit secrets lokal
.streamlit/secrets.toml
funders_cache.json
watchlist_pending.json
watchlist_robinhood_pending.json
# Snapshot/history Robinhood Chain disimpan ke branch holder-live saat cron;
# file lokal runner tidak perlu di-commit.
holder_status_robinhood.json
holder_history_robinhood.json
holder_history_robinhood.json.gz
# Journal likuiditas Pre-Pump Screener (diisi tiap run, ephemeral)
pre_pump_liq.json

# Cache hasil scan card (🏆 Best Pool Meteora / 🦅 Best Pool Krystal) supaya
# refresh browser tidak menghapus tabel. Isinya hanya angka pasar + alamat
# publik — API key TIDAK pernah disimpan di sini.
.scan_cache/

accumulation_history.json
# Setelan tombol notif Telegram (dipublish ke branch holder-live oleh UI/cron)
alert_settings.json

# Cache laporan RugCheck (rugchecker.cc) — hasil HTTP per mint, bukan sumber
# kebenaran; ditulis oleh modul rugchecker.py (permintaan user 2026-09-16).
rugchecker_cache.json

# Cache likuiditas total GMGN per mint — hasil HTTP, bukan sumber kebenaran;
# ditulis oleh modul gmgn_liquidity.py (permintaan user 2026-09-17).
gmgn_liquidity_cache.json
